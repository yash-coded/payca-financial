import styled from "styled-components";
import {
  space,
  layout,
  border,
  flexbox,
  grid,
  background,
  position,
  shadow,
} from "styled-system";

export const Container = styled.div`
  ${space}
  ${layout}
${border}
${flexbox}
${grid}
${position}
${background} 
${shadow} /* margin:0;
  padding: 0; */
`;
