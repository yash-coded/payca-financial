import styled from "styled-components";
import {
  typography,
  layout,
  space,
  color,
  border,
  position,
} from "styled-system";

export const Text = styled.p`
  ${typography}
  ${layout}
${space}
${color}
${position}
${border}
`;
