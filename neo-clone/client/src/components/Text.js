import styled from "styled-components";
import { typography, layout, space, color, border } from "styled-system";

export const Text = styled.p`
  ${typography}
  ${layout}
${space}
${color}
${border}
`;
